{% import "macros/premium.twig" as premium %}
{% extends "base.twig" %}

{% block page_title %}
{{ Message("stadiumenvironment_navlabel") }}
{% endblock %}

{% block page_content %}

<h3>{{ Message("stadiumenvironment_existingbuildings") }}</h3>

{% if existingBuildings|length == 0 %}
	{{ Message("stadiumenvironment_existingbuildings_none") }}
{% else %}
	<table class="table table-striped">
		<tbody>
			{% for building in existingBuildings %}
				{% if hasMessage(building.name) %}
					{% set buildingName = Message(building.name) %}
				{% else %}
					{% set buildingName = building.name %}
				{% endif %}
				{% if hasMessage(building.description) %}
					{% set buildingDescription = Message(building.description) %}
				{% else %}
					{% set buildingDescription = building.description %}
				{% endif %}

			<tr{% if building.under_construction %} class="warning"{% endif %}>
				<td>
				{% if building.picture|length > 0 %}
				<img src="{{ Config("context_root") }}/uploads/stadiumbuilding/{{ building.picture }}">
				{% endif %}
				</td>
				<td>
					<h4>{{ buildingName }}</h4>
					<p>{{ buildingDescription }}</p>
					{% if building.under_construction %}
						<p><strong>{{ Message("stadiumenvironment_existingbuildings_underconstruction",Datetime(building.construction_deadline, i18n)) }}</strong></p>
					{% endif %}
					{% if building.effect_income != 0 %}
						<p><strong>{{ Message("entity_stadiumbuilding_effect_income") }}:</strong>
						{{ building.effect_income|number_format(0, ',', ' ')  }} {{ Config("game_currency") }}</p>
					{% endif %}
				</td>
			</tr>
			{% endfor %}
		</tbody>
	</table>

{% endif %}

<h3>{{ Message("stadiumenvironment_availablebuildings") }}</h3>

{% if availableBuildings|length == 0 %}
	<div class="well">
	{{ Message("stadiumenvironment_availablebuildings_none") }}
	</div>
{% else %}

	<div id="buildingsSelection" class="carousel well slide" style="min-height: 200px">
	  <ol class="carousel-indicators">
	  	{% for building in availableBuildings %}
	    	<li data-target="#buildingsSelection" data-slide-to="{{ loop.index0 }}"{% if loop.index0 == 0 %} class="active"{% endif %}></li>
	    {% endfor %}
	  </ol>
	  <div class="carousel-inner">
	  	{% for building in availableBuildings %}
	  		{% if hasMessage(building.name) %}
				{% set buildingName = Message(building.name) %}
			{% else %}
				{% set buildingName = building.name %}
			{% endif %}
			{% if hasMessage(building.description) %}
				{% set buildingDescription = Message(building.description) %}
			{% else %}
				{% set buildingDescription = building.description %}
			{% endif %}
	    	<div class="item{% if loop.index0 == 0 %} active{% endif %}" style="padding: 0 50px">
				<h4>{{ buildingName }}</h4>
				<p>{{ buildingDescription }}</p>

				{% if building.picture|length > 0 %}
					<p><img src="{{ Config("context_root") }}/uploads/stadiumbuilding/{{ building.picture }}"></p>
				{% endif %}

				<dl class="dl-horizontal">
					<dt>{{ Message("entity_stadiumbuilding_costs") }}</dt>
					<dd>{{ building.costs|number_format(0, ',', ' ')  }} {{ Config("game_currency") }}
					{% if building.premiumfee %}
						{{ premium.premiumbadge(building.premiumfee) }}
					{% endif %}
					</dd>

					<dt>{{ Message("entity_stadiumbuilding_construction_time_days") }}</dt>
					<dd>{{ building.construction_time_days  }}</dd>

					{% if building.effect_income != 0 %}
						<dt>{{ Message("entity_stadiumbuilding_effect_income") }}</dt>
						<dd>{{ building.effect_income|number_format(0, ',', ' ')  }} {{ Config("game_currency") }}</dd>
					{% endif %}
				</dl>

				<p><a href="{{aUrl("order-building", "id=" ~ building.id) }}" class="btn btn-primary">{{ Message("stadiumenvironment_build") }}</a></p>
		    </div>
	    {% endfor %}
	  </div>
	  {% if availableBuildings|length > 1 %}
		  <a class="carousel-control left" href="#buildingsSelection" data-slide="prev">&lsaquo;</a>
		  <a class="carousel-control right" href="#buildingsSelection" data-slide="next">&rsaquo;</a>
	  {% endif %}
	</div>

{% endif %}

{% endblock %}