{% import "macros/badges.twig" as badgesmacros %}
{% extends "base.twig" %}

{% block page_title %}
{{ Message("badges_navlabel") }}
{% endblock %}

{% block page_content %}

<table class="table table-striped">
	<thead>
		<tr>
			<th>{{ Message('entity_badge') }}</th>
			<th>{{ Message('entity_badge_description') }}</th>
		</tr>
	</thead>
	<tbody>
	{% for badge in badges %}
		<tr>
			<td>
				{{ badgesmacros.badge(badge.name, badge.level) }}
			</td>
			<td>
				{% if badge.description|length and hasMessage(badge.description) %}
					{{ Message(badge.description) }}
				{% else %}
					{{ badge.description }}
				{% endif %}
			</td>
		</tr>
	{% endfor %}
	</tbody>
</table>

{% endblock %}