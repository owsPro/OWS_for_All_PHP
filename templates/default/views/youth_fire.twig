{% extends "base.twig" %}

{% block page_title %}
{{ Message("youthplayer-fire_navlabel") }}
{% endblock %}

{% block page_content %}
<form class="form-horizontal" method="post">

	<fieldset>
		<legend>{{ Message("youthplayer-fire_formlabel") }}</legend>
		
		<p><strong>{{ Message("youthplayer-fire_confirmationmsg") }}</strong></p>
	</fieldset>
	<div class="form-actions">
		<button type="submit" class="btn btn-primary">{{ Message('button_confirm') }}</button>
		<a href="{{ env.getInternalUrl("youth-team") }}" class="btn">{{ Message('button_cancel') }}</a>
	</div>
	<input type="hidden" name="page" value="youthplayer-fire"/>
	<input type="hidden" name="id" value="{{ env.getRequestParameter('id') }}"/>
	<input type="hidden" name="action" value="fire-youthplayer"/>
</form>
{% endblock %}
