
{% extends "base.twig" %}

{% block page_title %}
{{ Message("freeclubs_title") }}
{% endblock %}

{% block page_content %}

{% if countries|length > 0 %}

<div class="accordion" id="countries">
	{% for country,clubs in countries %}
		{% set nationImg = skin.getImage("flags/players/" ~ country ~ ".png") %}
		{% if hasMessage("option_" ~ country) %}
			{% set countryLabel = Message("option_" ~ country) %}
		{% else %}
			{% set countryLabel = country %}
		{% endif %}
	<div class="accordion-group">
		<div class="accordion-heading">
			<a class="accordion-toggle" data-toggle="collapse"
				data-parent="#countries" href="#collapse{{ loop.index }}">{% if nationImg %}<img src="{{ nationImg }}" alt="{{ countryLabel }}" /> {% endif %} {{ countryLabel }} ({{ clubs|length }})</a>
		</div>
		<div id="collapse{{ loop.index }}" class="accordion-body collapse">
			<div class="accordion-inner">
				<table class="table table-striped">
					<thead>
						<tr>
							<th>{{ Message("entity_club_name") }}</th>
							<th>{{ Message("entity_club_liga_id") }}</th>
							<th>&nbsp;</th>
							{% if Config("assign_team_automatically") %}
							<th>{{ Message("freeclubs_head_choose") }}</th>
							{% endif %}
						</tr>
					</thead>
					<tbody>
					{% for club in clubs %}
						<tr>
							<td>
								{% if club.team_picture|length %}
									<img src="{{ Config("context_root") }}/uploads/club/{{ club.team_picture }}" style="max-width: 40px"/>
								{% endif %}
								<a href="{{iUrl("team", "id=" ~ club.team_id ) }}">{{ club.team_name }}</a>
							</td>
							<td><a href="{{iUrl("league", "id=" ~ club.league_id ) }}">{{ club.league_name }}</a></td>
							<td>
								<small>
									<p><strong>{{ Message("entity_player_w_staerke") }}</strong><br>
									{% for strengthlevel in 1..5 %}
										<i class="icon-star{% if (30 + strengthlevel * 10) > club.team_strength %}-empty{% endif %}"></i>
									{% endfor %}</p>

									<p><strong>{{ Message("entity_club_finanz_budget") }}</strong><br>
									{{ club.team_budget|number_format(0, ',', ' ') }} {{ Config("game_currency") }}</p>

									<p><strong>{{ Message("entity_stadium") }}</strong><br>
									{{ (club.stadium_p_steh + club.stadium_p_sitz + club.stadium_p_haupt_steh + club.stadium_p_haupt_sitz + club.stadium_p_vip)|number_format(0, ',', ' ') }}</p>
								</small>
							</td>
							{% if Config("assign_team_automatically") %}
							<td>
								{% if env.user.clubId < 1 %}
									<a href="{{aUrl("choose-team", "teamId=" ~ club.team_id) }}" class="btn btn-small"><i class="icon-file-text-alt"></i> {{ Message("freeclubs_button_choose") }}</a>
								{% elseif Config("max_number_teams_per_user") > 1 %}
									<a href="{{aUrl("choose-additional-team", "teamId=" ~ club.team_id) }}" class="btn btn-small"><i class="icon-file-text-alt"></i> {{ Message("freeclubs_button_choose_another") }}</a>
								{% endif %}
							</td>
							{% endif %}
						</tr>
					{% endfor %}
					</tbody>
				</table>
			</div>
		</div>
	</div>
	{% endfor %}
</div>

{% else %}
	<p>{{ Message("freeclubs_no_free_clubs_available") }}</p>
{% endif %}

{% endblock %}