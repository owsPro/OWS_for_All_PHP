{% if Config("facebook_enable_comments") %}
	{% set url = env.getInternalUrl(null, "id=" ~ Request("id"), TRUE) %}

	<hr>
	<div class="fb-comments" data-href="{{ url }}" data-width="500"></div>
{% endif %}