{% import "macros/formelements.twig" as formelements %}

  {{ formelements.autocomplete("nick", Message("messages_recipient"), Request('nick'), true, validationMsg, "usernick-search") }}

  {{ formelements.textfield('subject', Message('messages_subject'), Request('subject'), true, validationMsg) }}

  {{ formelements.textarea('msgcontent', Message('messages_content'),Request('msgcontent'), true, validationMsg) }}
