{% import "macros/formelements.twig" as formelements %}
 
  {{ formelements.autocomplete("nick", Message("messages_recipient"), env.getRequestParameter('nick'), true, validationMsg, "usernick-search") }}
  
  {{ formelements.textfield('subject', Message('messages_subject'), env.getRequestParameter('subject'), true, validationMsg) }}

  {{ formelements.textarea('msgcontent', Message('messages_content'), env.getRequestParameter('msgcontent'), true, validationMsg) }}
  
